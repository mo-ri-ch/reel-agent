# Sound effects (optional)

The agent adds a **whoosh** on scene changes and a **pop** when a logo badge or big number appears.
Built-in sounds are used by default. To use your own, upload royalty-free files here named:

- `whoosh...mp3` (e.g. whoosh1.mp3, whoosh-soft.mp3). One is picked at random each reel.
- `pop...mp3` (e.g. pop1.mp3)

Pixabay's sound effects library (pixabay.com/sound-effects) is a good free source. Keep each file short (under 1 second).
